export let uid = 0;
export let oldRouterKey = ""; // 旧路由组件
export let oldComponentKey = ""; // 旧组件

// $Key: { $instance, $container, $oldVNode }
export const global = {};

export const baseVar = {
    uid: 0,
    oldRouterKey: "",
    oldComponentKey: "",
}

